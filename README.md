# Dependencias

* ## Las Dependencias necesarias para su funcion son las siguientes:
  * python con la version 3.10 se descarga con el add .PATH
> [!CAUTION]
> es necesario hacer una venv para su desarrollo con la version correcta
  >  ```py
  >    py -3.10 -m venv "nombre del entorno"
  * mediapipe la version 0.10.9 (en futuras versiones se cambia el .soultion por .task haciendolo obsoleto)
  * ```ps1
    pip install mediapipe==0.10.9
  * openCV
  * ```ps1
    pip install opencv-python

